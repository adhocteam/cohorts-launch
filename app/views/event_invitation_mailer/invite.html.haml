%p
  Hello #{@person.first_name},
%p= @event.description
%p= link_to 'Please click to setup a time for your interview', |
    calendar_url(token: @person.token, event_id: @event.id)    |
%p Thanks!
%p= ENV['TEAM_NAME']
%p
  If you'd like to be removed, just #{link_to "click here to unsubscribe.", public_person_deactivate_url(person_id: @person.id, token: @person.token)}
