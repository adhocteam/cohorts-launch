.row
  .span6
    = form_for(@program) do |f|
      %fieldset
        %legend Program
        - if @program.errors.any?
          #error_explanation.alert.alert-error
            %ul
              - @program.errors.full_messages.each do |msg|
                %li= msg
        = f.label :name
        = f.text_field :name, class: 'span6'
        = f.label :description
        = f.text_area :description, class: 'span6'
        .form-actions
          = f.submit 'Save', class: "btn btn-primary"
          = link_to 'Cancel', :back, class: "btn"
  .span6
    %p.well Programs are the societal issue that individual applications are attempting to address. "Safe passage to school" and "affordable broadband" are examples of Programs.
